$(document).ready(function())
  $("celeb-dating").submit(function(event) {
    const party = $("#party").val();
    const age = parseInt($("#age").val());
    const dates = $("dates").val();
  })
if (party === "club") {
$('showThisThing').show();
}